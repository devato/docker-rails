# ./docker-sync.yml
version: "2"
syncs:
  dockerapp-sync:
    sync_strategy: 'rsync'
    src: './'
    sync_host_port: 10872
    sync_args: '--inplace'
    notify_terminal: false
    #watch_args: '-v'
